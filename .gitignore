node_modules
dist
.DS_Store
*.local
.vite

# Visual-review output. These are regenerated by `npm run visual:review` from
# the working tree and from a git ref, so committing them would only add large
# binaries that go stale on the next renderer change.
artifacts
